####################################################################################################
# server
####################################################################################################
server:
  error: 
    include-exception: true
    include-stacktrace: always
  use-forward-headers: true

####################################################################################################
# spring
####################################################################################################
spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE
  profiles:
    active: debug
  thymeleaf:
    mode: HTML
#  output:
#    ansi:
#      enabled: ALWAYS
  security.oauth2.client.registration:
    google:
      client-id: xxxxx
      client-secret: xxxxx
    github:
      client-id: d158cfae2e8af3a9c152
      client-secret: 2525d98a71114a6037650de5530c41b8562cba22
      scope: user:email

  jmx:
    enable: false
  mvc:
    throw-exception-if-no-handler-found: true
  jpa:
    hibernate:
      ddl-auto: validate

####################################################################################################
# management
####################################################################################################
management.endpoints:
    jmx.exposure:
        include: '*'
        exclude: shutdown
    web.exposure:
        include: '*'
        exclude: shutdown
