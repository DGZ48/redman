####################################################################################################
# spring
####################################################################################################
spring:
  profiles:
    active: debug
  thymeleaf:
    mode: HTML
#  output:
#    ansi:
#      enabled: ALWAYS
  security.oauth2.client.registration:
          google:
            client-id: xxxxx
            client-secret: xxxxx
management.endpoints:
    jmx.exposure:
        include: '*'
        exclude: shutdown
    web.exposure:
        include: '*'
        exclude: shutdown
